# Script to generate the sequences.

# To use this script:

1. **Save the script** to a file (e.g., `export_sequences.sh`):

```bash
chmod +x export_sequences.sh
```

2. **Run the script** with database names:


```bash
./export_sequences.sh source_db_name target_db_name
```